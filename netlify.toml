[build]
  command = "npm run build"
  publish = "dist"

# Serve Decap admin as static files (before SPA fallback)
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
